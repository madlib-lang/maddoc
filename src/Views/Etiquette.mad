import type { Element } from "MadUI"
import {
  div,
  className,
  text
} from "MadUI"
import type { State } from "@/State"


Etiquette :: String -> Element State
export Etiquette = (content) =>
  <div className="definition__etiquette">
    {content}
  </div>
